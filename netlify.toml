# Studio Hanuri — Netlify build settings

[build]
  # 배포할 때 manifest.json을 자동 생성합니다.
  command = "node build-manifest.js"
  publish = "."

# index.html 외에 admin/도 그대로 서빙
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
